BPR (Builder Process Repo) 기능 정의서 v5
설계 철학
이 앱의 경쟁 상대는 다른 앱이 아니라 소장님 주머니 속 수첩과 현장 화이트보드다. 수첩은 줄 긋고 옆에 쓰면 1초다. 이 앱에서 같은 일을 하는 데 2초 이상 걸리면 설계를 다시 한다. 현장은 계획대로 안 가는 날이 일상이다. 비가 오면 오늘 할 일이 통째로 바뀐다. 어떤 기능이든 "계획이 틀어졌을 때도 빠른가?"를 기준으로 판단한다. 편하려고 만든 앱이 일이 되면 안 된다. 소장님이 고른 것만 움직이고, 자동으로 끼어드는 건 없다.

목적
현장 소장이 오늘 할 일을 한눈에 확인하고, 체크리스트 기록을 GitHub처럼 자산화해 다음 현장에서 재사용하며, 자동 일일 보고서를 생성하는 건설 현장 전용 앱.

페이지 구성
대시보드 (빈 상태 / 로그인 상태)
로그인
체크리스트
공정 저장소
일지 작성 (보고서)
사용자 여정
빈 대시보드(로그인 유도) → 로그인 → 대시보드(공정 없으면 등록 유도) → 공정 저장소에서 템플릿 선택 → 체크리스트에 불러옴 → 체크리스트에서 삭제/수정 가능 → 완성된 공정 구조를 공정 저장소로 저장 가능(메모는 제외하고 공정만 이동) → 체크리스트에서 대공정 누르면 모달로 소공정 열림 → 소공정은 대기/진행중/잔손보기/완료로 상태 관리 → 소공정 전체 완료 시 대공정도 완료 → 체크리스트에서 "오늘 할 일"로 지정하면 대시보드로 넘어감 → 대시보드에 표시된 소공정은 원본과 동일 객체이므로 상태가 바뀌면 체크리스트에서도 동기화 → 대시보드에서도 "보고서에 담기" 가능 → "보고서에 담기"는 잔손보기/완료 상태일 때만 가능

0. 랜딩페이지
서비스 소개 페이지. "시작하기" 버튼을 누르면 대시보드로 진입한다.

1. 로그인
별도 로그인 페이지를 둔다. 프론트에 로그인 전용 페이지와 라우터 가드를 세팅한다.

OAuth 틀: 네이버/카카오 로그인 버튼을 UI상 배치하되, 실제 연동은 개발 완료 후 진행. 현재는 비활성 상태로 "준비중" 표시.

테스트 로그인: "테스트 로그인" 버튼을 별도로 배치. 누르면 아이디 test1234 / 비밀번호 1234로 즉시 JWT 토큰 발급. 토큰 유효시간 24시간.

상단 표시: 로그인 전에는 오른쪽 상단에 [로그인] 버튼만 노출. 로그인 후에는 "노광수 소장님 [로그아웃]"으로 변경.

Redis 캐싱: 로그인 세션과 유저 정보를 Redis에 캐싱한다. TTL은 토큰 만료와 동일하게 24시간. 로그아웃 시 Redis 캐시 즉시 삭제. 토큰 만료 시 Redis 캐시도 자동 소멸.

캐시 정합성 처리: 로그아웃 시 프론트에서 React-Query 캐시 전체 초기화(queryClient.clear()). 로그인 시 이전 사용자 캐시가 남아있지 않도록 로그인 직후에도 React-Query 캐시 초기화 후 새 데이터 fetch. 토큰 만료 감지 시(401 응답) 자동으로 로그인 페이지 리다이렉트 + 캐시 초기화. 브라우저 새로고침(리렌더링) 시 Redis에서 세션 유효성 먼저 확인 → 유효하면 캐시 유지, 만료됐으면 로그아웃 처리.

2. 대시보드 (오늘의 현장)
앱의 메인 화면이다. 대시보드의 무게중심은 "지금 현장이 어디쯤인가"이며, "오늘 할 일"은 그 아래에 붙는 참고 목록이다.

로그인 전 (빈 대시보드)
레이아웃 구조는 다 보여주되, 데이터 자리에 "로그인을 해서 스케줄을 관리해보세요" 문구가 표시된다. 날짜, 날씨, 착공일, 준공일 자리는 비어있고, 진척도 영역에는 "로그인 시 진척도를 볼 수 있습니다" 문구가 들어간다. 로그인 유도 영역에 움직이는 캐릭터 배치.

로그인 후 (공정 없음)
공정 등록을 유도하는 문구 또는 캐릭터가 표시되며 공정 저장소 또는 체크리스트 직접 등록으로 안내한다.

로그인 후 (공정 있음)
오늘 날짜와 현장 날씨가 표시된다. 아래에 현장 정보(현장명, 주소)가 배치되고, 그 아래로 진척도·진행도·공정편차 영역, 오늘 할 일 영역, 빠른 작업 영역 순서로 구성된다.

진척도·진행도·공정편차 표시 영역
대시보드 상단에 원형(○) 게이지 3개가 나란히 배치된다. 각 원 안에 퍼센트(%) 수치가 들어가고, 원 위에 라벨이 표시된다.

① 진척도 (시간 기준)

원 위 라벨: 진척도 원 안: 퍼센트 수치 (예: 19%) 원 아래 부가 표시: "37일 경과 / 196일" 형식으로 경과일수와 전체 공사일수를 보여준다. 계산식: 착공일부터 준공예정일까지의 전체 공사 기간 중 오늘까지 경과한 일수의 비율이다. 예를 들어 196일 공사인데 37일이 지났으면 진척도 19%. 호버(모바일: 롱프레스) 시 툴팁으로 착공일과 준공예정일이 표시된다. (예: "착공일: 2026-01-05 / 준공예정일: 2026-07-20")

② 진행도 (작업 기준)

원 위 라벨: 진행도 원 안: 퍼센트 수치 (예: 24%) 원 아래 부가 표시: "10완료 · 5잔손 / 42" 형식으로 전체 소공정 개수 중 완료 및 잔손보기 개수를 보여준다. 계산식: 완료 = 1.0, 잔손보기 = 0.7, 진행중 = 0, 대기 = 0 가중치를 적용한다. (완료 개수 × 1.0 + 잔손보기 개수 × 0.7) / 전체 소공정 수 = 진행도.

③ 공정편차 (핵심 지표)

원 위 라벨: 공정편차 원 안: 퍼센트 수치 (예: -5%, +10%, 0%) 원 아래 부가 표시: 진행도가 진척도보다 앞서면 "공정 N개 앞서는 중", 뒤처지면 "공정 N개 완료 필요", 일치하면 "공정 0개 완료 필요"로 표시한다. 계산식: 진척도 − 진행도 = 공정편차.

공정편차 해석 기준: 진척도 50% − 진행도 50% = 0% → 딱 맞게 가고 있다. 진척도 50% − 진행도 30% = +20% → 시간은 흘렀는데 일이 늦다. 공기 지연 위험. 진척도 50% − 진행도 70% = −20% → 일이 빨라서 여유 있다.

공기지연 위험 표시: 공정편차가 양수(+)일 때 공기지연 위험 상태로 판단한다. 원형 게이지 색상이 경고색(빨간색 계열)으로 변하고 "⚠ 공기지연 위험" 경고 문구가 추가 표시된다. 공정편차가 0이면 정상(초록색 계열), 음수(−)이면 여유 상태(파란색 계열)로 각각 색상이 구분된다.

요약 메시지 바: 게이지 3개 아래에 한 줄 요약 메시지가 표시된다. (예: "↑ 작업 속도가 일정보다 5% 앞서고 있습니다 · 공정 2개 여유")

오늘 할 일
체크리스트에서 "오늘 할 일"로 지정한 소공정 항목들이 표시되는 영역이다. 대시보드에 표시된 소공정은 체크리스트의 원본과 동일한 객체다. 대시보드에서 상태를 변경하면 체크리스트에서도 즉시 동기화된다.

핵심 개념: 당일 아침에 체크리스트에서 오늘 처리할 소공정을 찍으면 이 영역에 실시간으로 반영된다. 전날 밤에 미리 지정해둘 수도 있고, 당일 아침에 현장 상황 보고 지정해도 된다. 어디까지나 "추천 목록"이지 확정된 계획이 아니다. 상황이 바뀌면 언제든 빼고 다른 걸 넣을 수 있다.

접기/펼치기: "오늘 할 일 (6)" 헤더를 탭하면 접기/펼치기 토글된다. 기본 상태는 펼침. 접힌 상태에서는 "오늘 할 일 (6)" 한 줄만 표시. 토글 애니메이션은 부드럽게(height transition).

비어있을 때: "체크리스트에서 오늘 할 일을 선택하세요" 안내 문구와 체크리스트 바로가기 버튼이 표시된다.

항목 표시: 각 항목은 소공정명, 소속 대공정명, 메모(있으면), 현재 상태(대기/진행중/잔손보기/완료)가 표시된다. 항목의 상태 색상은 체크리스트의 상태 체계와 동일하게 적용된다.

상태 변경: 대시보드에서도 소공정 상태 아이콘을 탭하면 대기 → 진행중 → 잔손보기 → 완료 → 대기로 순환한다. 변경 즉시 체크리스트 원본에 동기화된다.

보고서에 담기: 소공정 상태가 잔손보기 또는 완료일 때 "보고서에 담기" 버튼(보라색)이 노출된다. 대기, 진행중 상태에서는 버튼이 나타나지 않는다. 탭하면 해당 소공정이 메모와 함께 일지 작성 페이지로 이동한다.

인라인 제거: 각 항목을 왼쪽 스와이프하면 "제거" 버튼 노출. 탭하면 오늘 할 일 목록에서만 제거된다(소공정 자체 삭제 아님, 지정 해제). 제거 시 하단 토스트로 "실행 취소" 3초간 표시.

빠른 작업
대시보드 하단에 "체크리스트" 바로가기 버튼과 "일지 작성" 바로가기 버튼이 배치된다. 계획이 틀어졌을 때 체크리스트로 즉시 이동하는 탈출구 역할을 한다.

3. 체크리스트 (공정 관리)
대공정과 소공정 2단 구조다.

현장 정보 입력 (진입 조건)
공정 저장소에서 불러오지 않고 직접 체크리스트부터 시작하는 경우, 공정 등록 전에 현장명/현장주소/착공일/예상준공일을 먼저 입력받는다. 공정 저장소에서 이미 입력했으면 중복으로 받지 않는다.

대공정 카드
"마당타설", "기초공사" 같은 큰 단위의 공정이다. 고정된 목록이 아니라 소장이 현장에 맞게 자유롭게 추가하고 수정할 수 있다. 카드 자체에 진행도 바가 있어서 소공정 완료율에 따라 자동으로 채워진다. 카드를 누르면 모달로 소공정 목록이 열린다. 소공정이 전체 완료되면 대공정 카드도 자동으로 완료 상태가 된다.

소공정
대공정 안의 세부 작업이다. 예를 들어 "마당타설" 안에 터다지기, 비닐덮기, 레미콘 같은 항목이 들어간다. 소공정은 자유롭게 추가할 수 있는 버튼이 있다. 소공정 목록 안에서 사용자가 구분선을 자유롭게 삽입하여 작업 단계나 성격이 다른 항목들을 시각적으로 그룹화할 수 있다.

메모: 각 소공정 하단에 메모를 남길 수 있다. "레미콘 8시 예약 완료 / 작업시작 8시20분" 같은 현장 기록을 붙이는 용도다. 메모는 별도 페이지 이동 없이 해당 항목 바로 아래에서 펼쳐지고 접힌다. 메모는 체크리스트 안에서만 존재하며, 공정 저장소로 내보낼 때는 메모를 제외하고 공정 구조만 이동된다.

빠른 공정 등록: 소공정을 새로 추가할 때 입력어 기반 추천이 나타난다. 공정 저장소에 저장된 기존 공정 데이터를 기반으로 초성 검색 및 연관 공정 추천이 제공되어 빠르게 등록할 수 있다.

상태 체계
소공정 항목의 상태는 네 단계다. 상태에 맞는 색상 테두리가 표시된다.

대기(기본): 테두리 없음. 진행중: 파란 테두리. 잔손보기: 노란 테두리. 완료: 회색 테두리에 투명도 70~80% 처리(디아블로 퀘스트 완료 느낌).

소공정 하나라도 진행중으로 바뀌면 바깥 대공정 카드도 자동으로 진행중이 되면서 파란 그림자가 퍼지는 효과가 적용된다. 호버(모바일: 롱프레스) 시 더 진한색으로 변한다.

상태 변경 방식: 소공정 왼쪽 상태 아이콘을 탭할 때마다 대기 → 진행중 → 잔손보기 → 완료 → 대기로 순환한다. 별도 드롭다운이나 메뉴를 열지 않는다.

오늘 할 일 지정
소공정에 호버(모바일: 롱프레스)하면 "오늘 할 일로 지정" 체크 버튼이 나타난다. 여기서 체크한 항목이 대시보드 "오늘 할 일" 영역에 실시간으로 반영된다. 다시 호버(롱프레스)해서 체크를 해제하면 대시보드에서도 즉시 빠진다.

보고서에 담기
소공정 상태가 잔손보기 또는 완료일 때 "보고서에 담기" 버튼(보라색)이 노출된다. 대기, 진행중 상태에서는 버튼이 나타나지 않는다. 탭하면 해당 소공정이 메모와 함께 일지 작성 페이지로 이동한다. 소장님이 직접 고른 것만 보고서에 들어간다. 자동으로 넘어가는 건 없다.

공정 저장소로 내보내기
현재 프로젝트의 공정 구조(대공정 + 소공정 전체)를 이름 붙여서 공정 저장소로 통째 저장할 수 있는 버튼이 있다. 저장 시 대공정/소공정 구성과 구분선이 포함된다. 메모는 제외하고 공정 구조만 깔끔하게 이동된다. 메모는 해당 현장의 기록이지 공정 템플릿의 일부가 아니기 때문이다.

4. 공정 저장소 (Process Repo)
이 페이지가 BPR의 핵심 차별점이다. 현장마다 공정 구성이 다르고, 소장마다 노하우가 다르다. 매번 "어떤 작업이 있었지?" 하고 수첩을 뒤질 필요 없이, 한 번 등록한 공정 구조를 저장해두고 다음 현장에서 바로 불러쓴다.

디폴트 템플릿
templates 테이블에 is_default 컬럼(BOOLEAN)을 추가한다. is_default = true인 템플릿은 시스템 기본 템플릿으로, 모든 유저의 공정 저장소에 자동 노출된다. 유저 개인 템플릿(is_default = false)과 함께 표시되며, 기본 템플릿은 수정/삭제 불가하고 "가져오기"만 가능하다. 유저가 기본 템플릿을 가져오면 해당 유저의 프로젝트에 복사본이 생성되어 자유롭게 수정할 수 있다. 공정 저장소 조회 시 쿼리는 "WHERE user_id = 현재유저 OR is_default = true"로 처리한다.

현장 정보 입력 (공정 저장소 → 체크리스트 이동 시)
공정 저장소에서 공정 세트를 선택해 체크리스트로 가져갈 때 팝업이 뜨면서 현장명, 현장주소, 착공일, 예상준공일을 입력받는다. 현장주소는 직접 입력과 지도 기반 입력 두 가지를 모두 지원한다. 지도에서 위치를 찍으면 근처 주소가 자동 적용된다. 건설 현장 특성상 준공 전에는 정식 주소가 없는 경우가 많기 때문에, 지도 기반으로 대략적 위치를 잡아 임시 주소를 부여하는 방식이다.

공정 등록 및 검색
대공정/소공정을 미리 템플릿으로 등록해둔다. 초성 검색으로 빠르게 찾아서 현재 프로젝트에 추가할 수 있다.

전체 불러오기
저장해둔 공정 세트를 통째로 불러온 뒤, 이번 현장에 필요 없는 항목은 X 버튼으로 제거하고 필요한 건 추가한다.

체크리스트 → 공정 저장소 저장
체크리스트 페이지에서 완성한 공정 구조 전체를 이름 붙여서 저장소로 넘길 수 있다. 메모는 제외되고 공정 구조만 저장된다.

공정 저장소 → 체크리스트 불러오기
저장소에서 저장된 공정 세트를 선택해서 현재 프로젝트 체크리스트로 가져올 수 있다. 이때 현장 정보 입력 팝업이 뜬다.

입력어 기반 추천 데이터
공정 저장소에 축적된 대공정/소공정 데이터는 체크리스트에서 새 소공정을 추가할 때 추천 소스로 활용된다. 소장님이 글자를 입력하면 기존에 등록된 공정명을 기반으로 초성 검색 및 연관 추천이 나타나 빠르게 등록할 수 있다.

5. 일지 작성 (보고서)
원래 소장님이 수첩 보면서 손으로 옮겨 적던 일일 보고서를, 체크리스트에서 탭탭 찍어서 채우는 방식이다.

데이터 유입: 체크리스트 또는 대시보드에서 "보고서에 담기"를 탭한 소공정만 이 페이지에 들어온다. 자동으로 넘어오는 건 없다. 소장님이 고른 것만 들어간다. 담길 때 해당 소공정의 메모도 함께 이동된다.

보고서에 담기 조건: 소공정 상태가 잔손보기 또는 완료일 때만 "보고서에 담기" 버튼이 활성화된다.

자동 포함 정보: 대시보드에 있는 오늘 날짜, 현장 날씨 정보가 보고서에 자동으로 합쳐진다.

추가 입력: 사진 첨부와 간단한 추가 메모를 붙일 수 있다.

편집: 보고서에 담긴 항목을 빼거나 메모를 다듬을 수 있다. 보고서 안에서의 편집은 체크리스트 원본에 영향을 주지 않는다. 체크리스트는 소장님의 진짜 현장 기록이고, 보고서는 위에 보여줄 용도로 소장님이 편집한 버전이다.

인터랙션 원칙
수첩에서 지우개로 지우고 슥 쓰면 끝나는 속도를 디지털에서 재현하기 위한 조작 규칙이다. 모든 기능은 이 원칙을 따르며, 이 원칙에 어긋나는 설계는 다시 한다.

모든 수정은 탭 즉시 반영, 저장 버튼 없음. 소공정 이름을 누르면 그 자리에서 바로 커서가 뜨고, 다른 곳 누르면 자동 저장. "저장하시겠습니까?" 같은 확인 팝업은 일절 없앤다.

상태 변경은 원탭 순환. 소공정 왼쪽 상태 아이콘을 탭할 때마다 대기 → 진행중 → 잔손보기 → 완료 → 대기로 돌아간다. 별도 메뉴나 드롭다운을 열지 않는다.

삭제는 스와이프 한 번. 소공정을 왼쪽으로 밀면 삭제 버튼이 나오고 한 번 더 탭하면 끝. 되돌리기는 하단 토스트로 3초간 "실행 취소" 제공.

순서 변경은 길게 눌러 드래그. 소공정을 꾹 누르면 들려 올려지고 원하는 위치에 놓으면 끝.

메모는 인라인 펼침. 별도 페이지 이동 없이 해당 소공정 바로 아래에서 메모 영역이 펼쳐지고 접힌다.

탭 한 번에 안 되면 다시 설계한다. 이것이 모든 인터랙션 판단의 최종 기준이다.

UI 원칙 (50대 현장 소장 기준)
글씨와 버튼은 크게. 현장 햇빛 아래에서도 보이고, 거친 손으로도 터치 가능해야 한다. 한 화면에서 최대한 해결. 뎁스가 깊어지면 안 쓴다. 호버 기반 인터랙션은 모바일에서 롱프레스로 대체한다.