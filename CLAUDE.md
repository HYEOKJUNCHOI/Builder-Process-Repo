# BPR (Builder Process Repo) Development Ground Rules

이 문서는 BPR 프로젝트의 일관된 개발 경험과 코드 품질을 유지하기 위한 절대 원칙입니다. 모든 AI 엔진은 이 규칙을 준수하여 작업해야 합니다.

---

## 1. Stack & Environment
- **Web Frontend:** React 18+ (Vite), TypeScript
- **Mobile App:** React Native (Expo) — 현장 접근성 강화
- **State Management:** - Client: Zustand
  - Server: Tanstack React-Query (서버 상태 및 실시간 동기화)
- **Styling:** Emotion (css-in-js 방식)
- **Backend:** Spring Boot 3.5.x, Java 21 (Virtual Threads)
- **DB:** MySQL 8.0
- **Caching:** Redis (세션, 토큰, 유저 정보)
- **Auth:** JWT 토큰 인증 (Access/Refresh) + OAuth2 (Naver, Kakao — 준비 중)
- **AI Engine:** Claude API — 추후 보고서 자동화 등에 활용 예정 (현재 미적용)
- **Resolution:** 720px (모바일 퍼스트 기준)

---

## 2. File Structure & Role Rules
기능 단위 폴더 구조를 유지하며, 한 페이지를 작업할 때 필요한 모든 파일(화면, 스타일, API)을 한 폴더 내에서 관리한다.

### Directory Structure
- `pages/`: 페이지별 폴더 (Dashboard, Checklist, ProcessRepo, Report, Login)
  - `*.jsx`: 화면 컴포넌트 (렌더링 및 이벤트 핸들링만 담당)
  - `*.style.js`: Emotion 스타일 (S.접두사 사용)
  - `*.api.js`: 해당 페이지의 API 호출 함수 (axios 요청 정의)
- `components/`: 공통 컴포넌트
  - `common/`: Button, Modal, Toast 등 범용 UI
  - `layout/`: Header, BottomNav 등 레이아웃
- `hooks/`: 커스텀 훅 (useAuth, useChecklist 등)
- `store/`: Zustand 스토어
- `utils/`: 유틸리티 함수 (날짜 계산, 포맷 변환 등)
- `styles/`: GlobalStyle.js, theme.js (전역 스타일 및 테마)

### File Extension Rules
- **.jsx**: 화면 컴포넌트 전용. 비즈니스 로직이 길어지면 커스텀 훅으로 분리.
- **.js**: 설정, 인프라, 스타일, API, 유틸리티 등 JSX 문법이 필요 없는 모든 파일.
  - 예: `queryClient.js`, `axiosConfig.js`, `Dashboard.style.js`, `Checklist.api.js`

---

## 3. Coding Standards (Quality & Readability)
- **Naming Convention:**
  - 컴포넌트: `PascalCase` (예: ChecklistCard)
  - 함수/변수: `camelCase` (예: getProgressRate)
  - 스타일 컴포넌트: `S.PascalCase` (예: S.Container, S.CardWrapper)
  - API 함수: `fetch/create/update/delete` 접두사 (예: fetchChecklist)
  - 상수: `UPPER_SNAKE_CASE` (예: MAX_RETRY_COUNT)
- **Principles:**
  - "왜" 이렇게 작성했는지 의도를 설명하는 **한국어 주석** 필수.
  - 한 파일이 **300줄**을 넘으면 반드시 하위 컴포넌트로 분리.
  - 예외 상황(Error Handling)을 항상 고려하여 작성.

---

## 4. Communication & Persona
- **Language:** 모든 설명과 주석은 **'한국어'**로 작성한다.
- **Method:** 초보자도 이해할 수 있게 쉽게 설명하되, 비즈니스 로직과 구조를 명확히 짚어준다.
- **Proactive:** 요청에 잠재된 리스크나 더 나은 대안이 있다면 파트너로서 능동적으로 제안한다.
- **Format:** **[결론/해결책] → [코드] → [상세 설명]** 순서로 답변한다.
- **추론 흔적 (Breadcrumb):** 지시가 모호하거나 해석에 추가 추론이 필요했다면, 응답 끝에 다음 형식으로 반드시 알린다.
  ```
  🍞 추론 흔적: [무엇이 모호했는지] → [어떻게 쓰면 더 명확했을지]
  ```
  예: `🍞 추론 흔적: {}가 <> 없이 단독 사용됨 → <{현장명/주소, 내일날씨}>처럼 <> 안에 묶어주면 명확함`
  - **단, 해석이 2가지 이상이고 어느 쪽인지 확신이 없으면 추론하지 않고 바로 질문한다.** 🍞는 추론해서 진행했을 때만 남긴다.

---

## 5. Claude Code 전략
- **사용 도구:** Claude Code 단독 사용. 다른 AI 코딩 도구 혼용 없음.
- **모델 운용:**
  - `claude-opus-4-6`: 설계·뼈대·아키텍처 결정 단계
  - `claude-sonnet-4-6`: 일반 코드 구현 단계
- **모델 최적화 (양방향):**
  - 작업 시작 전 복잡도를 스스로 판단한다.
  - **낮춤 (비용 절감):** 단순 반복·소규모 수정이라면 낮은 모델로 전환을 제안한다.
    > 예: "이 작업은 `claude-sonnet-4-6`으로도 충분할 것 같습니다. 변경할까요?"
  - **올림 (품질 우선):** 설계 결정·아키텍처 변경·복잡한 트레이드오프 판단이 필요한 경우, 현재 Sonnet으로 작업 중이라면 Opus 전환을 제안한다.
    > 예: "이 작업은 구조적 판단이 필요합니다. `claude-opus-4-6`으로 변경하는 것을 권장합니다. 변경할까요?"
  - **단, 확신이 없는 경우 솔직히 이유를 밝힌다.** 잘못된 판단으로 비용 절감 또는 품질을 착각하게 만들지 않는다.
- **모델 변경:** 반드시 사용자 승인 후 진행한다. 임의로 변경하지 않는다.
- **No Arbitrary Changes:** 사용자가 태스크 진행 상황을 인지할 수 있도록, 부족한 부분 보강은 하되 기존 태스크를 임의로 변경하거나 생략하지 않는다.

---

## 6. DOM 식별 전략 (data-qa 규칙)

- **목적:** CSS-in-JS 환경에서는 className이 자동 생성되어 DevTools에서 코드 위치를 찾기 어렵기 때문에, 주요 DOM 요소에 `data-qa` 속성을 추가해 코드 위치를 즉시 식별할 수 있도록 한다.

- **적용 시점:** 새로운 페이지 또는 주요 UI 컴포넌트를 생성하거나 수정할 때, 해당 루트 요소에 `data-qa` 속성을 반드시 추가한다.

- **적용 범위:** 의미 있는 기능 단위의 **루트 컨테이너에만** 적용하며, 내부 하위 요소에는 무분별하게 추가하지 않는다.

- **적용 대상:**
  - 페이지 루트 div
  - `header`
  - `section`
  - `select`
  - `button`
  - 리스트 컨테이너 (`map` 함수의 부모 요소)
  - 주요 기능 단위의 최상위 컨테이너

- **명명 규칙:** `data-qa="페이지명-역할"` 형식을 사용하며, kebab-case를 사용하고 중복되지 않도록 한다.

- **예시:**
  - `data-qa="checklist-page"`
  - `data-qa="checklist-header"`
  - `data-qa="checklist-site-select"`
  - `data-qa="checklist-add-button"`
  - `data-qa="checklist-item-list"`

- **사용 제한:** `data-qa`는 스타일링 또는 로직 제어 목적이 아닌, DevTools 기반 위치 식별 및 유지보수 목적에만 사용한다.

- **기대 효과:** DevTools에서 즉시 코드 위치 식별이 가능해지고, UI 수정 속도와 AI 코드 수정 정확도가 향상되며 유지보수 비용이 감소한다.

---

## 7. 레이아웃 변경 규칙

### 7-1. 최소 변경 원칙 (기본)
- 사용자가 변경을 명시하지 않은 요소의 **위치, 크기, 패딩, 정렬, 줄 구조는 절대 변경하지 않는다.**
- 기존 요소를 임의로 **이동하거나 재배치하지 않는다.**
- 새로운 줄을 만들거나 기존 줄을 나누지 않는다.

### 7-2. 디자인 지시 프로토콜 (data-qa 기반)
사용자가 레이아웃을 변경하고 싶을 때는 `data-qa` 속성을 주소로 사용하여 지시한다. AI는 이 지시를 **문자 그대로** 따르며, 지시 범위를 넘어서는 변경을 하지 않는다.

**기호 체계:**

| 기호 | 이름 | 의미 |
|------|------|------|
| `[]` | 앵커 | 이미 있는 요소. 건드리지 않는다. 위치 참조용. |
| `<>` | 포인터 | 삽입 위치. "여기에 넣어라"라는 마우스 포인터. |
| `{}` | 이동 | 다른 곳에 이미 존재하는 요소를 여기로 옮긴다. |
| `{+}` | 생성 | 새로 만들어서 삽입한다. |
| `{*}` | 부유 배치 | 해당 요소를 `position` + `z-index`로 띄워서 다른 요소 위에 겹치게 배치한다. |

**조합 규칙:**
- `<{A}>` → A를 여기로 이동
- `<{A,B}>` → A와 B를 함께 여기로 이동
- `<{+A}>` → A를 새로 만들어서 삽입
- `<{A,+B}>` → A는 이동, B는 새로 생성하여 같이 삽입

**제약 사항:**
- `{}` 는 반드시 `<>` 안에서만 사용한다. `<>` 없이 단독으로 쓰면 삽입 위치가 불명확해진다.

**순서 규칙:**
- `<>` 안에서 쉼표(`,`)로 나열하면 **왼쪽부터 순서대로** 배치한다.
- `<{주소,날씨}>` → 주소가 왼쪽, 날씨가 오른쪽
- `<>`를 여러 개 찍으면 **왼쪽 `<>`가 1번**, 오른쪽으로 갈수록 순번 증가한다.

**지시 형식:**
```
data-qa="대상 컨테이너" 안에서
[기존요소A] <{이동할요소}> [기존요소B]
```

**예시 — 기존 요소 이동:**
```
data-qa="checklist-header" 안에서
[체크리스트] <{주소,날씨}> [토글] [삭제]
```
→ 주소와 날씨를 체크리스트와 토글 사이로 이동. 주소가 왼쪽, 날씨가 오른쪽.

**예시 — 새 요소 생성:**
```
data-qa="dashboard-header" 안에서
[노광수님] <{+오늘날짜}> [로그아웃]
```
→ 오늘 날짜를 새로 만들어서 노광수님과 로그아웃 사이에 삽입.

**예시 — 이동 + 생성 혼합:**
```
data-qa="dashboard-header" 안에서
[노광수님] <{날씨,+오늘날짜}> [로그아웃]
```
→ 날씨는 기존 위치에서 이동, 오늘 날짜는 새로 생성하여 함께 삽입.

**예시 — 각각 다른 위치에 삽입:**
```
data-qa="checklist-header" 안에서
[체크리스트] <{주소}> [토글] <{날씨}> [삭제]
              ↑ 1번째              ↑ 2번째
```
→ 주소는 체크리스트와 토글 사이에, 날씨는 토글과 삭제 사이에 각각 배치.

**예시 — 수치 조정:**
```
data-qa="checklist-header"의 padding-top을 20px로 변경해줘
```
→ 해당 요소의 스타일만 정확히 수정.

**픽셀 넛지 기호 (CSS 오프셋 이동):**

| 기호 | 이름 | 의미 |
|------|------|------|
| `{요소} -Npx` | 왼쪽 넛지 | 해당 요소를 N픽셀 왼쪽으로 민다 (`margin-left: -Npx`) |
| `{요소} +Npx` | 오른쪽 넛지 | 해당 요소를 N픽셀 오른쪽으로 민다 (`margin-left: Npx`) |

- DOM 위치 변경 없이 **시각적 위치만 픽셀 단위로 이동**하는 CSS 오프셋 지시이다.
- 이 경우에만 `{}` 를 `<>` 없이 단독 사용할 수 있다. (DOM 재배치가 아닌 CSS 오프셋임을 명확히 구분)

**예시 — 왼쪽으로 이동:**
```
{대공정목록} -70px
```
→ 대공정목록 네비게이션을 왼쪽으로 70px 이동. (`margin-left: -70px`)

**예시 — 오른쪽으로 이동:**
```
{알림아이콘} +20px
```
→ 알림 아이콘을 오른쪽으로 20px 이동. (`margin-left: 20px`)

### 7-3. 대규모 레이아웃 재구성이 필요한 경우
기존 구조를 **"재설계"** 수준으로 변경해야 할 때는 다음 절차를 따른다:
1. AI는 즉시 실행하지 않고, **변경 범위와 영향받는 요소 목록**을 먼저 보고한다.
2. 사용자가 승인한 후에만 실행한다.
3. 변경 전후의 구조를 주석이나 커밋 메시지로 기록한다.

---

## 8. 협업 프로토콜

### 8-1. 협업 구조
- **메인:** Claude Code (CC) — 주 작업 도구
- **서브:** Antigravity / Gemini (AG) — Claude Code 쿼터 소진 시 교대 투입
- 역방향 교대도 가능 (AG 쿼터 소진 시 CC로 복귀)

### 8-2. 핸드오프 규칙
- 사용자가 **"핸드오프 해"** 라고 지시하면 즉시 핸드오프를 수행한다.
- AI가 스스로 판단해서 핸드오프를 하지 않는다. **반드시 사용자 지시가 있어야 한다.**
- 핸드오프 절차:
  1. 현재 작업을 **안전한 상태까지 마무리**한다. (컴파일 에러 없는 상태)
  2. `git add → commit → push` 수행한다.
  3. 프로젝트 루트의 `HANDOFF.md`를 업데이트한다.
  4. HANDOFF.md도 함께 커밋한다.

### 8-3. 인수인계 절차
- `HANDOFF.md`가 있으면 → 읽고 시작 (사용자가 @으로 띄워줄 수 있음)
- `HANDOFF.md`가 없으면 → 코드를 직접 읽고 변경점을 파악한 뒤 작업을 이어간다.
- 인수인계 받으면 **브랜치를 새로 파서** 작업을 시작한다.

### 8-4. 브랜치 전략 (Git Flow 간소화)

**브랜치 구조:**
```
main                ← 배포 가능한 안정 버전
  └── develop       ← 개발 통합 브랜치
        ├── feature/cc-checklist-drag-sort
        ├── fix/ag-dashboard-sync-bug
        └── hotfix/cc-login-token-expire
```

**브랜치 네이밍:**
```
접두어/작업자태그-설명(kebab-case)
```

| 접두어 | 의미 |
|--------|------|
| `feature/` | 새 기능 개발 |
| `fix/` | 버그 수정 |
| `hotfix/` | 긴급 수정 |
| `refactor/` | 구조 개선 (동작은 그대로) |

| 작업자 태그 | 도구 |
|-------------|------|
| `cc` | Claude Code |
| `ag` | Antigravity (Gemini) |

**교대 시 브랜치 바톤 터치:**
```
feature/cc-checklist-drag       ← CC가 작업
       ↓ 핸드오프
feature/ag-checklist-drag       ← AG가 브랜치 새로 파서 이어받음
       ↓ 핸드오프
feature/cc-checklist-drag-v2    ← CC가 다시 이어받음
```
브랜치가 바톤 역할을 하므로, 코드트리에서 누가 어디까지 했는지 한눈에 추적할 수 있다.

### 8-5. 커밋 메시지 규칙

**형식:**
```
[작업자태그] 타입: 설명
```

**예시:**
```
[CC] feat: 체크리스트 드래그 정렬 구현
[CC] style: 소공정 상태별 테두리 색상 적용
[AG] feat: 대시보드 오늘 할 일 연동
[AG] fix: 날씨 API 응답 파싱 수정
```

| 타입 | 의미 |
|------|------|
| `feat` | 새 기능 |
| `fix` | 버그 수정 |
| `style` | 스타일 변경 (동작 무관) |
| `refactor` | 리팩토링 (동작 무관) |
| `docs` | 문서 수정 |
| `chore` | 설정, 빌드 등 기타 |
