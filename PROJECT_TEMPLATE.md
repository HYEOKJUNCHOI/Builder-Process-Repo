# PROJECT_TEMPLATE.md — 프로젝트 청사진 템플릿

> 이 템플릿은 AI와 협업하여 프로젝트를 만들 때 사용하는 **프로젝트 설계 문서**의 범용 틀이다.
> 각 섹션의 설명을 읽고, 자기 프로젝트에 맞게 채운 뒤 `PROJECT.md`로 이름을 바꿔 사용한다.
> 모든 섹션을 채울 필요는 없다. 핵심(★)은 반드시 채우고, 나머지는 프로젝트 규모에 따라 선택한다.

---

## ★ 1. 프로젝트 개요

> AI가 이 프로젝트를 처음 보는 순간, 10초 안에 "아 이런 거구나"를 파악하는 섹션.

- **프로젝트명:** (예: BPR — Builder Process Repo)
- **한줄 요약:** (예: 건설 현장 소장이 오늘 할 일을 관리하고, 공정 기록을 자산화하는 모바일 앱)
- **버전:** (예: v1.0 / MVP / 프로토타입)

---

## ★ 2. 설계 철학

> 이 프로젝트의 모든 판단을 관통하는 핵심 원칙.
> "이걸 왜 만드는가?" "어떤 기준으로 판단하는가?"를 적는다.
> 구체적일수록 AI가 엉뚱한 방향으로 가지 않는다.

(예: "이 앱의 경쟁 상대는 다른 앱이 아니라 수첩이다. 수첩보다 느리면 설계를 다시 한다.")

---

## ★ 3. 타겟 유저

> 누가 쓰는가? 이 사람의 환경, 능력, 제약을 적는다.
> AI가 디자인과 UX를 결정할 때 이 정보를 기준으로 삼는다.

- **주요 사용자:** (예: 50대 건설 현장 소장)
- **IT 숙련도:** (예: 카카오톡, 네이버 수준 / 개발자 수준 / 완전 초보)
- **사용 환경:** (예: 햇빛 아래 현장, 거친 장갑 낀 손, 720px 모바일)
- **사용 빈도:** (예: 매일 아침 현장 출근 시 + 퇴근 전 보고서 작성 시)
- **핵심 불편:** (예: 매번 수첩 뒤져서 공정 기록 찾는 게 번거로움)

---

## ★ 4. 핵심 기능 목록

> 이 서비스가 하는 일을 나열한다. 우선순위를 매기면 AI가 뭘 먼저 만들지 판단할 수 있다.

| 우선순위 | 기능 | 설명 | 비고 |
|----------|------|------|------|
| P0 (필수) | (예: 체크리스트) | (예: 대공정/소공정 2단 구조로 작업 관리) | |
| P0 (필수) | (예: 대시보드) | (예: 오늘 할 일 + 진척도 한눈에 확인) | |
| P1 (중요) | (예: 공정 저장소) | (예: 공정 구조를 저장/불러오기) | |
| P1 (중요) | (예: 일지 작성) | (예: 체크리스트에서 탭탭 찍어 보고서 생성) | |
| P2 (추후) | (예: OAuth 로그인) | (예: 네이버/카카오 소셜 로그인) | 현재 테스트 로그인으로 대체 |

---

## ★ 5. 사용자 여정 (User Flow)

> 사용자가 앱에 들어와서 목표를 달성하기까지의 흐름.
> 화살표(→)로 이어서 적는다. 분기가 있으면 분기도 적는다.

```
(예)
첫 진입 → 빈 대시보드 → 로그인 유도 → 로그인
→ 대시보드(공정 없음) → 공정 등록 유도
→ 공정 저장소에서 템플릿 선택 → 체크리스트에 불러옴
→ 소공정 상태 관리 → "오늘 할 일" 지정 → 대시보드에 반영
→ "보고서에 담기" → 일지 작성 → 보고서 완성
```

---

## ★ 6. 페이지별 기능 정의

> 각 페이지(화면)가 무엇을 보여주고, 어떤 동작을 하는지 상세하게 적는다.
> AI가 실제로 코드를 작성할 때 이 섹션을 기준으로 만든다.
> 페이지 수만큼 반복한다.

### 6-0. 랜딩페이지
(서비스 소개, 시작하기 버튼 등)

### 6-1. 로그인
(인증 방식, 토큰 관리, 캐시 전략 등)

### 6-2. 대시보드
(상태별 화면 구성: 로그인 전 / 로그인 후 공정 없음 / 공정 있음)

### 6-3. 메인 기능 페이지
(핵심 CRUD, 상태 체계, 데이터 흐름)

### 6-4. 보조 기능 페이지
(보고서, 설정, 프로필 등)

---

## 7. 인터랙션 원칙

> 모든 UI 조작의 기준이 되는 규칙.
> "이 원칙에 어긋나면 다시 설계한다" 수준의 절대 규칙을 적는다.

(예)
- 모든 수정은 탭 즉시 반영. 저장 버튼 없음.
- 상태 변경은 원탭 순환. 별도 메뉴나 드롭다운 없음.
- 삭제는 스와이프 한 번. 되돌리기는 하단 토스트 3초.
- 탭 한 번에 안 되면 다시 설계한다.

---

## 8. UI 원칙

> 디자인의 판단 기준. 타겟 유저의 환경과 능력을 고려한 규칙.

(예)
- 글씨와 버튼은 크게. 최소 터치 영역 44×44px.
- 한 화면에서 최대한 해결. 뎁스가 깊어지면 안 쓴다.
- 호버 기반 인터랙션은 모바일에서 롱프레스로 대체한다.
- 확인 팝업("저장하시겠습니까?")은 일절 없앤다.

---

## 9. 데이터 모델 (선택)

> DB 테이블 구조나 주요 엔티티 관계를 적는다.
> 백엔드 작업 시 AI가 이 정보를 기준으로 API와 스키마를 설계한다.

```
(예)
User ─── Project ─── Checklist ─── Task
                         │
                    ProcessRepo (템플릿)
```

| 엔티티 | 주요 필드 | 관계 |
|--------|-----------|------|
| (예: User) | (예: id, name, email) | (예: 1:N Project) |
| (예: Project) | (예: id, name, startDate, endDate) | (예: 1:N Checklist) |

---

## 10. 기술 제약 / 비기능 요구사항 (선택)

> 성능, 보안, 호환성 등 기능 외의 요구사항.

- **해상도:** (예: 720px 모바일 퍼스트)
- **오프라인:** (예: 오프라인 모드 필요 여부)
- **성능:** (예: 페이지 로딩 2초 이내)
- **보안:** (예: JWT 인증, HTTPS 필수)
- **호환성:** (예: 최신 Chrome/Safari, iOS 15+, Android 10+)

---

## 11. 마일스톤 / 로드맵 (선택)

> 언제까지 뭘 만들 건지. AI에게 작업 범위를 알려주는 용도.

| 단계 | 목표 | 기한 | 상태 |
|------|------|------|------|
| Phase 1 | (예: 로그인 + 체크리스트 MVP) | (예: 2주) | 진행중 |
| Phase 2 | (예: 대시보드 + 공정 저장소) | (예: 3주) | 대기 |
| Phase 3 | (예: 일지 작성 + 배포) | (예: 2주) | 대기 |

---

## 12. 용어 사전 (선택)

> 프로젝트에서 사용하는 도메인 용어를 정의한다.
> AI가 코드에서 네이밍할 때 이 용어를 기준으로 삼는다.

| 용어 | 영문 | 의미 |
|------|------|------|
| (예: 대공정) | (예: majorProcess) | (예: 마당타설, 기초공사 같은 큰 단위의 공정) |
| (예: 소공정) | (예: subProcess / task) | (예: 대공정 안의 세부 작업) |
| (예: 잔손보기) | (예: touchUp) | (예: 거의 끝났지만 마무리가 남은 상태) |

---

## 작성 가이드

### ★ 표시된 섹션 (1~6)
프로젝트 규모와 관계없이 **반드시 작성**한다. AI가 코드를 쓸 때 이 정보가 없으면 엉뚱한 방향으로 갈 수 있다.

### 나머지 섹션 (7~12)
프로젝트 규모에 따라 **선택적으로 작성**한다. 없어도 AI가 작업할 수 있지만, 있으면 품질이 올라간다.

### 작성 팁
- **구체적으로 적을수록** AI가 정확하게 만든다. "좋은 UX" 대신 "탭 한 번에 상태 변경"처럼.
- **예시를 포함**하면 AI가 의도를 정확히 파악한다.
- **안 하는 것도 적어라.** "자동 저장 없음", "확인 팝업 없음"처럼 명시적 제외도 중요하다.
- 업데이트할 때마다 **버전 번호**를 올려서 AI가 최신 정보를 참조하게 한다.
